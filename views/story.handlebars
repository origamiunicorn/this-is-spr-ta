<div class="row story-row">
    <div class="col s12">
        <p class="parabody">
            {{data.body}}
        </p>

        <div class="divider story-divider"></div>

        {{#each data.Choices}}
        <p>
            <a href="/story/{{dataValues.routeId}}">{{dataValues.description}}</a>
        </p>
        {{/each}}
    </div>
</div>